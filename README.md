# poc-common-model

Common domain models shared across services.

## Purpose
This repository contains reusable model definitions that are consumed by other services and libraries.  
It acts as an upstream dependency for downstream repositories.

## Current Version
See the `VERSION` file for the current released version.

## Structure
# poc-common-model
